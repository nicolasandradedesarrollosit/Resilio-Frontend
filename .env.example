# Example environment variables for local development
# Copy this file to .env.local (or .env) and fill in the real values.

# URL of the backend API the frontend will call (adjust port if your backend runs elsewhere)
VITE_API_URL=http://localhost:4000

# Supabase project configuration (replace with your project's values)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...your_anon_key_here

# Optional: additional VITE_* variables can be added here as needed
